#Import the module into a new program and use the function to calculate the square of a number.

import module

num = int(input("Enter a number: "))
result = module.square(num)

print("Square of the number:", result)
